# Zombie Escort TD — リリース工程表 v3.7（2026-08-16 / 判定基準改訂・P3 2本目スタンプ版）

**この文書の役割**: リリースまでの共有レール。僕（小松）・Claude・CC・Opus/Sonnetの誰が見ても「今どこ／残りいくつ」が分かるスタンプラリー。各フェーズに完了定義（＝スタンプ条件）を付す。期限は設けない（進捗はスタンプで測る）。

**関連ドキュメント（必読）**:
- **spec05_game_identity.md（ゲームの人格章）** — このゲームが何であるかの北極星。新機能・演出・調整の是非判断は必ずこの章との整合で行う。一枚看板は「家族を読みで守り抜く、静かな脱出劇」
- **spec07_engagement_recipe.md（交戦地レシピ・型カタログ）** — ステージ設計を「配置を考える」から「型を選んで刺す」に変える基礎資料。撃破時間・交戦時間・必要基数の確定式、initial型カタログ、Gから来る型の正解タワー位置公式を収録。P3量産の実作業の土台
- **file_structure_review.md（ファイル構成レビュー）** — コードベース構造の俯瞰と改修計画（R-1〜R-5）。実行タイミング一覧を「現在地」更新時に必ず照合すること
- **segment_schema_draft.md（区間スキーマ叩き台v0.1）** — 新文法（区間制・位置トリガー）のJSON仕様
- spec05（設計ガイド本体）

**v3.7の変更点（判定基準改訂・P3 2本目スタンプ・2026-08-16・小松判断による確定事項改訂）**: **ステージ合否の判定基準を「良100%配置の存在証明」の一点に統合**。雑100%不成立の確認とCLOSE_CALL 0回=設計NGを両方とも合否判定から外した——どちらもプレイヤーの配置次第で数字が動き、ステージ自体の性質を測る道具になっていなかったため（Claudeが組む雑配置は初見プレイヤーの外し方と一致せず、良い配置でもCLOSE_CALLが0回になることが外周ぐるりの実測で判明）。spec06_stage_recipe.md §7の判定基準・spec05_game_identity.mdのCLOSE_CALL目安表を改訂し、工程表の確定事項・P3スタンプ条件（良100%配置の存在証明＋実機確認＋index.json登録の3条件に整理）にも反映。外周ぐるり（stage_free_03）がP3の2本目としてスタンプ確定（良100%配置12基でHP残100%全員、実機確認は小松プレイで面白さ合格、難易度は確定ABAステージ=10とした相対順位で5）。2-2構成台帳に実際のステージ行（1本目・2本目）を初めて記入。既に検証済みの2ステージは雑100%不成立の確認も実施済みのため、スタンプの遡及的な見直しは不要。スタンプ進捗を17/60→18/60に更新。

**v3.6の変更点（P3制作フロー順序変更・2026-08-15・小松判断）**: P3のステージ制作フローを「検証してから実機プレイ」から**「実機プレイしてから検証」の順序に変更**。新フロー: ①設計→JSON作成→R-6→index.json登録 ②小松が実機でプレイ（面白さ判定・フィードバック） ③フィードバック反映（必要なら②に戻る） ④良100%配置の存在証明 ⑤雑100%不成立の確認 ⑥P3スタンプ付与。「外周ぐるり」（P3 2本目）で良100%配置の探索に複数回のシム反復を要した後に実機フィードバックで設計が変わる可能性が残る状態になったことを受けた判断——面白さのフィードバックで設計が変わると、先に済ませた検証（工程内で最もコストが高い）がやり直しになるため。留意点として、実機で「難しすぎる」となった場合に設計自体の問題か配置の問題かを切り分けるため、Claudeが配置を1本組んで実証してから判断する運用も明記。P3のスタンプ条件自体（良100%配置の存在証明＋雑100%不成立の確認＋実機確認＋index.json登録）は変更なし、実施順序のみの変更。

**v3.5の変更点（P3 1本目スタンプ・2026-08-14）**: 2-9（資金ルールの修正）実装完了——escortごとの支給分(grantMoney)と稼ぎ(earnedMoney)を分離し、交代時に支給分残りのみ消滅させる方式に変更（GameScene.js/run_sim.js/simulator.htmlの3系統。既存6ステージは無影響、確定ABAステージの良100%配置は無改造で成立を確認済み）。確定ABAステージ（stage_free_02）の雑100%不成立確認が完了（3パターンとも100%未達）し、P3のスタンプ条件（良100%配置の存在証明＋雑100%不成立の確認＋実機確認＋index.json登録）を全て満たしたため、**P3スタンプ[1/20]を確定**。スタンプ進捗を16/60→17/60に更新。「現在地」を本日時点に更新し、次にやることを「2本目のステージ制作」のみに整理。

**v3.4の変更点（R-6完了・2026-08-11）**: **validate_stage.jsを3便構成で実装完了**（コミットe149600/cebd8d0/cb9b9e1、file_structure_review.md反映5d21bef）。1便目=座標・ルール突き合わせ9項目（spawn道路上・spawn/pathゼロ距離接触防止・initial敵数上限・propと道路の衝突・枠外spawn着地先の道路/path判定・buildSpotの道路/path判定・グリッド範囲・資金供給元ゼロ検出・enemy hp直書き検出）。2便目=枠外spawnの侵入所要時間 vs segment猶予のERROR/WARN判定（項目10）、および`--margin`オプションによる全escort×全waypointの猶予マップ出力（判定なし・補助モード）。3便目=`file_structure_review.md`のR-6原文にあり1・2便目で見落とされていた残り3項目（buildSpots×propフットプリント衝突・spawnキー未定義参照検出・leashTo座標の歩行可能性）。確定ABAステージで全項目OKを確認、各項目とも意図的に壊したテストJSONでERROR/WARNが正しく出ることを確認済み。これにより`docs/file_structure_review.md`のR-6原文6項目・工程表の旧R-6検査項目10件リスト、両方が完全にカバーされた。リスク登記簿9番（JSONの書き損じが実行結果からしか発覚しない）を解消済みとして記録。「現在地」からR-6を除去し、次にやることを2-9（資金ルール修正）／確定ABAステージの雑100%不成立確認／2本目のステージ制作の3項目に更新。

**v3.3の変更点（逆順生産撤回・確定ABAステージ登録・2026-08-02・小松指示による確定事項改訂）**: **【最重要】P3の作成順序「ステージ20から逆順で作る」を撤回**——難易度が事前に測れない以上、天井（何が最難か）も事前には決められないことが実践で判明したため。**代わりに1本ずつ作り、遊び、体感で相対順位をつける方式に転換**（Lv番号は後から割り振る）。あわせて確定事項に3点追記: ①**遺産タワーが難易度の第3の軸**として働き、家族数・敵種の2軸と逆向きに作用する（前の家族の敵を強くするほど後の家族が楽になる、確定ABAステージ rev1→rev2で実測）②**敵数は難易度レバーとして弱い**（交戦数が同じでも遺産の濃淡・導線間隔・交戦地の分散で体感難易度が大きく変わることを実測）③**エンタメと難易度を別軸で扱う**（脅威の敵＝難易度／演出の敵＝すり抜け前提でエンタメ、を明確に分離）。あわせて**枠外スポーンの設計ルール**（侵入所要時間<segmentの猶予）を確定事項に新設。P2は2-7・2-8を完了チェックし2-9（資金ルール修正）を新設、2-2を「作る前の設計図」から「作りながら埋まる台帳」に位置づけ変更してP3着手の前提から外した。P4に4-6（開始前ルール表示）・P5に5-6（リレー交代の導線提示フェーズ）を新設。確定ABAステージ（stage_free_02）をP3の1本目としてstages/index.jsonへ登録（良100%配置の存在証明とPCブラウザでの実機確認は完了、雑100%不成立の確認が未実施のためP3スタンプは保留）。リスク登記簿にリスク9（JSONの書き損じが実行結果からしか発覚しない）を新設し、リスク1（タッチ精度）に本日解決したタップ処理不具合3件を追記。本改訂はすべて小松の判断・指示によるもの（工程表の更新権限ルール上のCC単独禁止事項に該当するが、本依頼をもって承認済みとして扱う）。

**v3.2の変更点（Y一撃離脱実装・2026-07-29）**: spec07 §9-4で確定した仕様「Y中(waveNum==='Y')のゾンビは護衛への攻撃が1回成立した時点で消滅する」を実装。Zombie.jsの攻撃処理1箇所への追加のみで3系統（GameScene.js/run_sim.js/simulator.html）全てに反映される（waveNumが単一共有ファイルで管理されているため）。撃破カウント・reward付与はなし。CLOSE_CALL集計からは除外（攻撃成立時点で必ず150px以内にいるため、除外しないと「Y中は常にCLOSE_CALLが立つ」形になり0回=設計NGの判定が反転してしまう）。「すり抜け」とは別に`[Y_HIT_BY_SPAWN]`・RESULT行`Y一撃離脱=N体`として集計（すり抜け集計からも除外し二重計上を防止）。

**baseline更新（回帰基準の変化）**: 既存6ステージのうちYを実際に使用する`stage_お父さん、寄り道中.json`のシム結果が変化した（GAMEOVER→CLEAR、HP残0%→70%）。他5ステージはRESULT行に`Y一撃離脱=0体`が追加されるのみで数値は完全に不変。2026-07-29の一撃離脱実装により、Y使用ステージの数値が変化。Y中の被害が青天井だった状態が解消され、このステージは初めてCLEAR可能になった。劣化ではなく仕様通りの効果のため、この新しい数値を回帰baselineとする。

**v3.1の変更点（設計側の記録を同期・2026-07-29）**: 2026-07-25〜28に確定した設計方針が工程表に未反映だったため一括で同期。①難易度の数値化を放棄し「レベル10を作って引き算で下位を作る」方式へ転換（2-2の再定義・確定事項の修正）②交戦地レシピ（型カタログ）をspec07_engagement_recipe.mdとして新設し、正典5本目・引き継ぎ5点セットに追加③CLOSE_CALL指標の運用を改訂（0回=設計NGのみ合否に使い、1回以上の回数は合否に使わない。見るべきは発生箇所数）④trigger型の「スポーン後2秒の無敵」発覚と「Gから来る型」の正解位置公式の確定を記録⑤2-8（CLOSE_CALL箇所数の出力）を新設。

**v3.0の変更点（シムprop衝突ゲート実装・2026-07-27）**: 「シムが衝突の最終ゲートキーパー」（spec05_stage_design.md記載）が実際にはR-6発覚時点（2026-07-18）から未実装のままだった穴を解消。run_sim.js/simulator.htmlのタワー建設処理（即時・delayed両方）に、propフットプリントとの衝突判定を追加——衝突する座標への設置は`[ERROR]`ログを出して**設置そのものをスキップ**する（そもそも成立しない配置として扱う）。実装中に別バグを発見・修正: run_sim.jsの`ctx.PROP_DEFS`が常に`undefined`を返していた（Node vmの既知の癖——トップレベルconst/letがコンテキストの`ctx.NAME`プロパティに反映されるかは束縛ごとに不安定で、`TOWER_DEFS`/`GROUND_BLOCK_DEFS`等は偶然露出していたがPROP_DEFSは露出していなかった）。修正は`vm.runInContext('PROP_DEFS', ctx)`でコンテキスト内評価に切り替える形。既存6ステージの無タワー回帰確認・stage_test_06での衝突検知/非衝突配置の両方を確認済み。**GameScene.js（実機）側は今回のスコープ外**（依頼が「シムで回す際」に限定されていたため）——実機でも同じ抜け穴が残っているため、シム=実機一致の鉄則に照らすと別途対応が必要。

**v2.9の変更点（鳥・侵入フェーズ非交戦化・2026-07-26）**: 型検証（鳥8体・sniper1基）のシムで、盤外を飛行中の鳥が旋回開始の2秒前に全滅する事象を発見——鳥は護衛範囲円の適用対象外（v2.5確定）のため、射程内でありさえすれば侵入中・盤外を問わず常時交戦対象になっていたことが原因。旋回演出はプレイヤーが「気づいて備える」ための読み時間として設計されているため、旋回が始まる前に決着すると演出が無意味化する。**対応: 鳥（_flying個体）を侵入フェーズ（`_birdPhase===null`）の間はタワーのターゲット候補から除外**（無敵化ではなく候補除外——弾が飛んで当たらない状態は作らない）。Tower.js/simulator.html/run_sim.jsの3系統の`_findNearest`相当ロジックに同一条件を追加。旋回フェーズ・ホーミングフェーズの交戦仕様（護衛範囲円の適用対象外・対空タワー制限）は無変更。stage_鳥プレビュー.jsonでCLI検証: 侵入中は`[FIRE]`が一切出ず、`[MAP_ENTER]`（侵入完了・旋回開始）と同時刻から`[FIRE]`が発生することを確認。既存6ステージ全てでシム完走（エラーなし）を確認。**これはv2.5確定事項（鳥のみ護衛範囲円の適用対象外）を維持したままの追加制約であり、確定事項の取り消しではない**（小松判断済み）。

**v2.8の変更点（敵HP再配分・2026-07-26）**: P2 2-2カーブ表着手前の前提整理として、balance.json（＋config.js ZOMBIE_BASEフォールバック）の敵HPをノーマル攻撃力50の倍数へ統一——salaryman 75→100（2発）／worker 100→150（3発）／police 150→200（4発）／kickboard 60→50（1発）／bird 60→50（1発）／debu 300据え置き（6発）。旧設定はHP60/75/100がノーマル2発に丸められ敵種の重さが差別化されていなかったため、1・2・3・4・6発ときれいに散るよう再配分（speed/damage/rewardは変更なし）。GameScene.js/simulator.html/run_sim.jsの3系統にHPのハードコードは無いことを確認済み（全てZOMBIE_BASE/balance.json経由）。stages/index.jsonの実働6ステージ全てでCLIシム完走（エラーなし）を確認。**【発見・小松判断で解決済み】既存6ステージのenemy定義は`"hp":200`等を個体ごとに直接ハードコードしており、balance.jsonの今回の変更の影響を受けない（例: stage_本番テスト（読める盤面）.jsonのsalaryman全箇所が`hp:200`固定）。判断: 既存6本はP3逆順生産で全て作り直す検証用テスト台（製品には残らない）かつシム＝実機一致の回帰基準として現状維持したいため、**既存ステージは変更しない（凍結）**。新規に作る最近のステージ（hpを明示していない構成）には今回の変更がそのまま反映される。今後のルール: **新規ステージJSONのenemyにhpを書かない（balance.json値を継承）。意図的に個体を強化・弱体化する場合のみ明示指定を許可し、その際は_commentで理由を必ず残す**（spec06 §9地雷14として記録・R-6のチェック項目にも追加）。既存ステージのシムbaseline（RESULT数値）は変更なし（既存ステージのhpは不変のため実質無影響）。

**v2.7の変更点（P1完了・2026-07-24）**: **1-7（統合検証ステージ）クローズ。P1[10/10]完了、次はP2。** 新設ステージで「5人リレー（dad/mom/son/grandma/cat）＋区間制＋導線A/B＋敵全種（salaryman/worker/police/kickboard/bird/debu）」を全部載せし、シムで護衛生還5/5・スポーン455体全数消化・撃破441・すり抜け14・CLOSE_CALL15回・判定CLEARを確認——全機能が同居して破綻しないことを実証（**「機能の統合検証」としての完了であり、ステージ20としての設計＝面白さ仮説・良配置の設計は別途未完了**、P3の逆順生産着手時に本設計を行う）。あわせて未記録だった前回セッション分の変更もここに記録: ①ブロック型道路タイル（`道縦ノーマル (2)`等）のroad-only歩行可能判定漏れをGameScene.js/simulator.html/run_sim.jsの3系統で修正（描画は展開済みだが判定側が原点セルのみだった不整合。影響確認: 既存5ステージ中3ステージでシム結果変化＝正当な補正、baseline要更新）②editor.htmlの`buildJSON`に`lanes`フィールドを追加し、導線A/Bの生形状を家族割当と無関係に常時保持③シムのMAX_TIME（10分固定）を可変化——`opts.maxTimeMs`(CLI)/`stage.simMaxTimeMs`(JSON)で上書き可能に（優先順位: CLI＞JSON＞既定600000ms、run_sim.js/simulator.html両系統・回帰影響なし確認済み）。**重要な設計上の発見**: 新文法（segments/位置トリガー）では護衛speedと難易度の関係が旧文法から反転している（詳細はP2セクション参照、2-2カーブ表設計に直結）。`segment_schema_draft.md`（工程表必読リストに参照はあるが実体が無かった）を作成済み。

**v2.6の変更点（導線A/Bエディター実装・2026-07-24）**: P2/P3セクションに「要検討」として記載していた導線A/B振り分けのeditorツールを実装・小松実機確認済み。複数path描画（導線A=シアン・導線B=マゼンタ）・escort→導線割り当てUI・A/Bの交差/重複対応・既存ファイル読込時の自動A/B判別を実装（詳細はP2セクション「導線A/B振り分け設計」参照）。**書き出されるステージJSONの形式・ランタイム側は無変更**（エディター内部の編集モデルのみの変更）。実機確認: 2 WAVE構成でS/G共通・A/B別ルート・(19,9)での交差を含むステージを作成し、出力JSONの両escort path配列が正しく独立・連結していることを確認済み。

**v2.5の変更点（1-5完了・2026-07-23）**: 1-5「新敵3種」クローズ（小松実機確認）。デブはburgerスプライト（完成済み1体目・高精細・サイドビューのみ）を正式に使用するよう変更——旧`burger`（サラリーマン相当の仮パラメータ）エントリは削除し`debu`（HP300/速度25/damage20/reward50）に統合、仮のテキストラベル表示も撤去。鳥のベース値をHP30→60・damage8→12・reward15→30に確定（HP60はnormal2発分の硬さが基準）。確定事項セクションの護衛範囲円の項目を改訂し、「鳥（_flying個体）のみ例外」を正式記載（v2.1の「例外なしの共通ルール」からの初の改訂）。敵のベースパラメータは全て暫定値であり、P2の2-2カーブ表作成時にまとめて見直す方針を明記。次工程は1-7（統合検証ステージ）。

**v2.4の変更点（1-5鳥系実装・2026-07-23）**: 新敵「鳥」（bird）を実装・実機確認済み。侵入(盤外→旋回地点円周上へ直進)→旋回(circleRadius=見た目サイズ・circleLoops=周回数・circleDurationMs=秒数の3独立パラメータ)→ホーミング(護衛へ直線追尾)の3フェーズをZombie.js内の状態分岐として実装、FlowField/prop衝突は完全無視。旋回終了フレームでの即攻撃フォールスルーを塞ぎ、旋回中は絶対に攻撃されないことを保証。絵は羽ばたき2枚を進行方向へ回転させる方式（くちばし先端が原点・4方向別スプライト不要）。対空タワー制限（cannon/ice/punchは鳥を無視・normal/sniperのみ交戦可）をTower.js/simulator.html/run_sim.jsの3系統に実装。**さらに鳥のみ護衛範囲円(escortEngageRadius)の適用対象外に変更——v2.1確定事項（護衛範囲円は例外なしの共通ルール）に対する初の例外を小松判断で正式決定**（旋回が自前の遅延を持つため範囲円との二重制約を回避）。デブ（仮絵・パラメータのみ登録）も追加。GameScene.js側の`_spawnZombie`がenemyDefを独自再構築しflying/circleAt等を落としていたバグ（run_sim.js/simulator.htmlはenemyDefをそのまま渡す実装のため気づかず実機でのみ再現）も修正済み。既存ステージのシム回帰は全て完全一致を確認。

**v2.3の変更点（1-4b Y単体テスト完了・2026-07-21）**: Y再実装（新文法版）の実装＋検証が完了し、1-4・1-4bをクローズ。Y単体テストステージ「お父さん、寄り道中」（editorで作成）をCLIシムで検証: wallet着席取得（waiting開始の瞬間に固定額自動取得）・yInflow時間駆動発火（Y開始からの経過msで複数波・複数方向から発火）・建設ロック（Y中の建設は[WARN]でスキップ・Y後は通常通り）・retreat（Y終了時の残敵退場）を全て確認。**設計上の留意点が判明**: 護衛範囲円(escortEngageRadius:450px)はY中も有効なため、Yの防衛範囲は必然的にY地点周辺450px圏に限定される（yInflowのスポーン地点は450px圏内に収めること）。シム=実機の一致も既存ステージ同様に問題なし。次工程は1-5（新敵3種）または1-7（統合検証）、優先順は検討中。

**v2.2の変更点（1-4.5 GO確定・2026-07-19）**: 1-4.5「新文法実証実験」— **GO確定**（小松の実機判定）。根拠: ①良100%配置の存在証明（stage_honban_test.json dad区間、確定配置`normal:14,2 normal:15,4 normal:15,9 cannon:4,8 cannon:4,16 normal:9,16 normal:15,16`で HP残100%・CLOSE_CALL2回・最接近101px・撃破16/16・すり抜け0・生還1/1、実機で決定論一致確認済み）②雑=100%不成立の確認（異なる4パターンの雑配置いずれも100%到達なし: 0%/0%/8%/83%、spec06 §7絶対条件を充足）③7/18実機照合（見える版2周目）で「ちゃんとゲームになっている」の総評。判定基準はspec06_stage_recipe.mdの改訂版（良=HP100%クリア可能配置の存在が絶対条件／雑=100%不成立をLv別目標帯で確認／並=観測のみ）に準拠。次工程は1-4（Y再実装・新文法版）→1-4b→1-5。

**v2.1の変更点（挙動確認フェーズ完了・2026-07-17）**: 護衛範囲円（escortEngageRadius:450・タワーは円内の敵しか交戦しないゲート）を新規実装・実機で快感成立を確認。脅威の文法を2種に最終確定（initial待機＋盤外歩き込み侵入、「湧く」廃止）。墓デカールを飾りに格下げ（スポーン連動描画は全廃）。盤外スポーン実装（グリッド外座標→歩き込み・カメラはマップ端クランプ維持）。34体わらわら配置の実機テスト成功（処理負荷問題なし）。run_sim.jsのvm束縛潜在バグ（let宣言COLS/ROWSが外部代入で更新されない）を発見・修正。挙動確認フェーズ完了、1-4.5は本番segment_test再設計の段階へ。

**v2.0の変更点（新文法転換・2026-07-17）**: スポーン文法を時間駆動（spawnEvents）から**区間制・位置トリガー（segments）**へ転換する実証実験を開始。1-4/1-4bを✅から差し戻し（旧Y実装のsetSpawnMultiplier方式は廃案）、1-4.5「新文法実証実験」を新設（撤退基準付き）。1-7を新文法版に差し替え。スタンプ進捗は11/55→9/55に後退（量産前に仕様の手戻りを検出できた健全な後退）。CLOSE_CALL指標実装済み。工程表の更新権限ルールを新設（本文末尾）。ゲームの人格を確定しspec05_game_identity.mdとして文書化。

**v1.9以前の変更履歴**: v1.9=倍速バグ修正（1-6完了）／v1.8=護衛個体パラメータ・HPバー修正（1-2完了）／v1.7=escortDef別wave・run_sim.js整備（1-1/1-8完了）／v1.6=設置コスト改定・アップグレードシステム実装（1-3完了）／v1.5=タワー実装完了確認・rangeTest再計算／v1.4=sniper射程修正450→350px／v1.3=Y実装完了（※v2.0で差し戻し）／v1.2=タワー5種体系確定／v1.1=CC実装レビュー反映

**ゴール**: Android買い切り300円・一発リリース。20ステージ＋家族リレー＋Y＋ボーナスステージ、フル搭載完成版。

**確定事項（ぶれない前提）**:
- **【v3.1改訂】20ステージ。難易度は数値化しない**——プレイヤーによって体感が変わるため、数字での定義は行わない（2026-07-25・小松判断）。プレイヤーが初見で「前より難しくなった」と判断する要素は**①家族数②敵種③敵数④MAPサイズ**の4つで、これは測る物差しではなく満たすべき条件として扱う。**【v3.3撤回】「ステージ20を先に作り引き算で下位を作る」逆順生産は撤回**（詳細は【P3方針】参照）
  - **【v3.3追記・実測】敵数は難易度レバーとして弱い。** 確定ABAステージのrev1とstage_free_01を比較すると、実際に交戦した敵数はどちらも35体で同一（前者はスポーン47・すり抜け12、後者はスポーン40・すり抜け5）にもかかわらず、体感は「簡単」→「明確に難しい」に変わった。効いたのは①遺産の濃淡②導線間隔（A導線から6マス離すと遺産が効かない）③交戦地の数と分散（CLOSE_CALL発生箇所が4→9）
- 家族リレー＝基礎文法（MAX5人: dad/mom/son/grandma/cat、ステージごとに人数構成可変）
- クリア条件: 1人でもゴールでCLEAR／全滅NOT CLEAR。生存人数でポイント（＝メタ通貨）
- 後続の家族ほど重い（護衛パラメータ＋ゾンビ強さの2軸）
  - **【v3.3追記】遺産タワーが第3の軸として働き、しかも2軸目と逆向きに作用する。** 共有導線では、前の家族の敵を強くするほどプレイヤーがその区間に多くタワーを建て、結果として後の家族が楽になる。実測（確定ABAステージ rev1→rev2）: dad区間の敵をworker（HP150）→police（HP200）に格上げしたところ、sonのHP残が56%→100%に上昇した。後続を重くしたい場合、敵を強くするだけでは逆効果になりうる。有効なのは「前の家族が素通りした区間に後の家族の交戦地を置く」＝遺産の濃淡を作ること
- **【v2.2確定】スポーン文法: 区間制・位置トリガー（segments）。1-4.5のGO判定（2026-07-19・小松実機判定）により正式採用確定**
- **区間（シーン）制: リレー成立時に前区間の残敵は画面外へ退場（retreat）。失敗の蓄積は区間内に閉じる**
- **【v2.1確定・v2.5改訂】護衛範囲円（交戦ゲート）: 護衛中心・半径escortEngageRadius(450px・balance.json)の円内にいる敵しかタワーは交戦しない。開幕スナイプ防止＋「円に触れた瞬間の起動」が見せ場として成立（実機確認済み）。円半径はsniper射程350pxより大きく保つこと。**【v2.5で例外追加】鳥（_flying個体）のみ範囲円の適用対象外（旋回フェーズが自前の遅延を持つため、範囲円との二重制約を回避する目的。小松判断で正式決定・2026-07-23）。この項目の「例外なしの共通ルール」は鳥に限り撤回、他の敵種には引き続き例外なく適用。**【v2.9で範囲を明確化】この適用対象外は旋回・ホーミングフェーズのみに限る。侵入フェーズ（`_birdPhase===null`・盤外〜旋回開始前）はそもそもターゲット候補から除外されるため範囲円の判定自体が発生しない（2026-07-26）**
- **【v2.5確定】対空タワー制限: 鳥（_flying個体）を交戦対象にできるのはnormal/sniperのみ。cannon/ice/punchは鳥を最初からターゲット候補から除外し、非交戦（Tower.js/simulator.html/run_sim.jsの3系統に実装済み）**
- **【v2.1確定】脅威の文法は2種のみ: ①initial待機（開幕から盤面に見えている・護衛円で起動・待機中は完全静止＋非交戦）②盤外からの歩き込み侵入（progressトリガー・グリッド外座標発生→歩行状態で画面端から現れる）。「その場で湧く」スポーンは廃止**
- **【v2.1確定】墓デカールは飾り（スポーン予告の意味なし）。スポーン地点への自動描画は全廃、置く場合はprops側で任意配置**
- Y（寄り道・静的TDモード）: ステージ内で家族1人だけ発動、誰が/有無は初見不明。**【v2.0で再定義】旧setSpawnMultiplier方式は廃案。Y地点紐付けの専用流入（yInflow）として1-4.5 GO判定後に再実装**。世界観上の意味づけ=家族が日常のつもりで寄り道（公園休憩・買い物）
- ステージ内タワーアップグレード採用（リレー成立の部品）
- タワーは新5種: normal/sniper/cannon/ice/punch。仕様確定・実装完了済み（【タワー確定パラメータ】参照）
- **採点思想（v2.0確定）: 生存人数が主軸。撃破数主軸は不採用（退場演出との矛盾・ノーダメ美学との不整合のため）。残マネーは初期リリースでは参考表示のみ。倒す快感は採点でなくゲームフィール（P5-4）で担保**
- メタ強化は「ご褒美」幅に絞る方向（Q2壁打ちで確定）
- 敵は4種: salaryman（実装済）＋鳥系（prop無視飛行）＋デブボス＋キックボード
- ボーナスステージ1種（ロジック流用ミニゲーム）——切り離し可能・最後尾
- 鉄則: シム=実機の完全一致（1×基準）。対症療法禁止。ズレは根本原因まで追う。**シム=シム一致（CLI/ブラウザ/単独実行/連続実行の一致）も同鉄則に含む**
- **【v3.1確定】スポーン後2秒の無敵**: 全ゾンビはスポーン後2000ms、タワーのターゲット候補から除外される（Zombie.js `_spawnTimer` ＋ Tower.jsのターゲット選定5箇所のガード）。護衛範囲円と同種の開幕スナイプ防止機構。**initial型には影響しないが、trigger型では撃破時間に2秒が上乗せされる**（詳細はspec07 §2-6）
- **【v3.1確定→v3.7破棄】CLOSE_CALLの運用**: 「0回は設計NG判定に使う」を**破棄**（2026-08-16・小松判断）。配置が上手いときにも0回になることが実測で判明したため——外周ぐるりの良100%配置（タワーを敵の湧く場所の真横に固めた構成）はCLOSE_CALL 0回でHP100%クリアだったが、同ステージの小松実機プレイでは8回/7箇所発生している。CLOSE_CALLは**良い配置ができたときの緊張度を見る参考値**に格下げし、合否判定には一切使わない。見るべきは発生箇所数（詳細はspec05_game_identity.md）
- **【v3.3確定】エンタメと難易度は別軸で扱う。** 敵には2つの役割がある。
  - **脅威の敵** = 難易度を作る。すり抜けたら意味がない。必ず交戦させる設計が要る
  - **演出の敵** = エンタメを作る。来ることに意味があり、倒せなくてもすり抜けても成立する。難易度の計算からは最初から除外する（iceとファイヤーボムを計算外に置くのと同じ扱い）
  - 実証: 確定ABAステージrev2で枠外突入の7体が全すり抜けし難易度に一切寄与しなかったが、小松の体感は「最後のsonのGに向けての大激戦」だった。HP残もすり抜け数も体験の濃さを測る指標にならない
  - 注意: 演出の敵がたまたま届いて難易度に効くと読めなくなるため、届いても致命傷にならない設計にする（弱い敵種・数を絞る）。演出にデブや警察を使うのは危険
- **【v3.3確定】枠外スポーンは「侵入所要時間 < segmentの猶予」を満たすこと。**
  - segmentの`onExit`はコード上一度も読まれておらず、区間を抜けた敵は値に関わらず無条件でretreatする。区間をまたいで敵を残す仕組みは存在しない
  - 侵入所要時間 = 盤外距離(px) ÷ 敵のspeed。着地後に交戦圏(450px)へ寄る時間も別途必要
  - 実務ルール: 盤外spawnは盤から1〜2マスに置き、triggerは区間の開始と同時に発火させる。速い敵（kickboard 90）を遠くに、遅い敵（worker/debu 25〜50）を近くに割り当てる
  - 失敗例（rev1）: 2〜4マス外に置き区間途中で発火させた結果、12体全てがすり抜けた

---

## 全体マップ（7フェーズ）

```
P0 基盤完結 → P1 コアシステム → P2 バランス基盤 → P3 ステージ量産
                                                        ↓
リリース ← P6 ストア/仕上げ ← P5 外装 ← P4 メタ/外周ゲーム
```

**クリティカルパス**: P0→P1→P2→P3。ここが総工程の背骨で、特に P1（コアシステム）と P3（量産）が最重量。P4以降は一部P3と並行可能。

---

## P0: リレー基盤の完結 【完了 4/4✅】

| # | タスク | 担当 | 状態 |
|---|---|---|---|
| 0-1 | リレー実機動作確認（escorts2要素・父母） | 済 | ✅ |
| 0-2 | シムのリレー対応（1×基準・4000ms固定） | 済 | ✅ |
| 0-3 | シム三層検証（良100/並60/雑GAMEOVER・±16ms） | 済 | ✅ |
| 0-4 | 1×実機ログ取得→シムと照合（2人目区間±数ms） | 小松→Claude検算 | ✅ |

---

## P1: コアシステム実装 【完了 10/10✅・2026-07-24】

ゲームの面白さの本体。ここが固まるまでステージ量産に入らない（手戻り防止）。

| # | タスク | 内容 | 担当 | 依存 |
|---|---|---|---|---|
| 1-1 ✅ | escortDef別wave | `def.spawnEvents ?? stage.spawnEvents`フォールバック実装（GameScene.js/simulator.html）。※新文法GO判定後はsegmentsフォールバックに同様の考え方を継承 | CC | 完了 |
| 1-2 ✅ | 護衛個体パラメータ | balance.json escortsセクション（dad:HP120/spd50・mom:HP90/spd65・son:HP80/spd70・grandma:HP70/spd35・cat:HP60/spd75）・ESCORT_DEFS・HPバーdepth5専用gfx修正 | CC | 完了 |
| 1-3 ✅ | ステージ内タワーアップグレード | 3段階（Lv1→Lv2→Lv3）。balance.json/config.js/Tower.js/GameScene.js/simulator.html全実装済み（v1.6） | CC | 完了 |
| **1-4.5 ✅** | **新文法実証実験 — GO確定（2026-07-19・小松実機判定）** | 実装基盤（SegmentManager／retreat／護衛範囲円／initial待機／盤外スポーン）が全て揃い、本番segment_test再設計（stage_honban_test.json）で検証完了。判定根拠: ①良100%配置の存在証明（確定配置でHP残100%・撃破16/16・すり抜け0、実機で決定論一致確認済み）②雑=100%不成立の確認（4パターンいずれも100%未達: 0%/0%/8%/83%、spec06 §7絶対条件を充足）③7/18実機照合（見える版2周目）で「ちゃんとゲームになっている」の総評。判定基準はspec06_stage_recipe.md（良=HP100%クリア可能配置の存在が絶対条件／雑=100%不成立をLv別目標帯で確認／並=観測のみ）に準拠。仕様の正典: spec06_stage_recipe.md＋本表の【v2.1確定】3項 | Claude設計→CC実装→小松実機判定 | 完了 |
| 1-4 ✅ | Y再実装（新文法版） | 【v1.3の✅を差し戻し→再実装完了】旧setSpawnMultiplier方式を廃止し、Y地点紐付けの専用流入（yInflow）として再設計・実装完了。Escort.jsのdetourステートマシン（walking→detouring→announcing→waiting→returning）を流用、onDetourActivate/Endでwallet加算・buildLocked切替・YInflowManager起動/retreatを配線。run_sim.js/simulator.html両方に同一ロジックをミラー実装済み | Claude設計→CC実装 | 完了 |
| 1-4b ✅ | Y単体テストステージ（新文法版) | editorで作成した「お父さん、寄り道中」ステージでCLIシム検証完了。wallet着席取得・yInflow時間駆動発火（複数波・複数方向）・建設ロック・Y終了時retreatを確認。**護衛範囲円(450px)がY中も有効なため、Y防衛範囲はY地点周辺450px圏に限定される制約が判明**（下記【Y設計の留意点】参照） | 小松×Claude×CC | 完了 |
| **1-5 ✅** | **新敵3種 — 完了（2026-07-23・小松実機確認）** | **鳥系**: 侵入→旋回→ホーミングの3フェーズ（Zombie.js内の状態分岐、FlowField/prop完全無視）。旋回は半径(circleRadius・見た目サイズ)と周回数(circleLoops)を分離した独立パラメータ制（既定: 半径150px・周回数2.5・旋回秒数6000ms）。絵は羽ばたき2枚(閉/開)を進行方向へ回転させる方式（4方向別スプライト不要・くちばし先端が原点）。対空タワー制限（cannon/ice/punchは鳥を無視・normal/sniperのみ交戦可）と、鳥のみ護衛範囲円(escortEngageRadius)の適用除外（v2.1確定事項に対する初の例外・確定事項セクション参照）をTower.js/simulator.html/run_sim.jsの3系統に実装済み。**キックボード**: スプライト4方向完成済み（バウンス演出＋土埃パーティクル）。**デブ**: 完成済み1体目の高精細スプライト（`burger`、サイドビューのみ・左右反転で運用）を使用、HP300/速度25/damage20/reward50。**敵4種すべて実機確認済みで1-5完了**。**【暫定値の注記・2026-07-26更新】敵HPはv2.8でノーマル攻撃力50の倍数に再配分済み（詳細はv2.8変更点参照）。speed/damage/rewardは引き続き暫定値のまま** | CC＋スプライトはGPT | 完了 |
| 1-6 ✅ | 倍速バグ根本修正 | `intervalTimer -= delta` → `totalDt`（1行）。2倍速時のリレーインターバル連動を修正 | CC | 完了 |
| **1-7 ✅** | **統合検証ステージ（新文法版） — 機能統合検証として完了（2026-07-24）** | 新設ステージとして「5人リレー（dad/mom/son/grandma/cat）＋区間制＋導線A/B＋敵全種（salaryman/worker/police/kickboard/bird/debu）」を全部載せし、シムで**護衛生還5/5・スポーン455体全数消化・撃破441・すり抜け14・CLOSE_CALL15回・判定CLEAR**を確認。全機能が同居して破綻しないことを実証。**【重要】この✅は「機能の統合検証」の完了を指し、ステージ20としての設計（面白さ仮説・良配置の存在証明）は別途未完了**——本設計はP3で1本ずつ作りながら行う（下記【P3方針】参照）。新文法（segments）ベースで構築（旧spawnEvents前提の記述は無効） | 全員 | 1-4.5 GO・1-4〜1-6 |
| 1-8 ✅ | run_sim.js技術負債の解消 | run_sim.js整備済み。※v2.0-2.1期間中の追加改修: vm Node.js 24対応（class→var Foo=class）・Phaserスタブ・CLOSE_CALL重複カウント修正・SegmentManager分岐・**vm束縛潜在バグ修正（let宣言COLS/ROWSが外部代入で束縛更新されずデフォルト15固定→20×20マップで縁スポーン誤判定。vm.runInContextでの再代入に修正、既存ステージ基準値と完全一致確認済み）** | CC | 完了 |

**スタンプ条件**: 1-4.5でGO判定が出た上で、1-7の全部載せステージがシム=実機一致で三層成立（1×＋倍速）。**＝「Lv5基準ステージの完成形」が新文法で実在する状態。** **【2026-07-24】達成・P1完了。** ただし1-7クローズの根拠はCLIシム結果（護衛生還5/5・撃破441・すり抜け14・CLOSE_CALL15回・判定CLEAR）のみで、実機（1×＋倍速）での確認は別途必要な場合は追って実施。

> **【Y設計の留意点・2026-07-21】** Yの防衛範囲は護衛範囲円(450px)によりY地点周辺に限定される。yInflowのスポーン地点はY地点から450px圏内に収まるよう設計すること（圏外に置いても敵はタワーの交戦対象にならない）。

### CLOSE_CALL指標（実装済み・v2.0新設）

「成立する」と「面白い」は別物。三層検証は成立の担保、CLOSE_CALLはギリギリ感の近似指標。

- 定義: ゾンビが護衛の至近（CELL×1.5=150px以内）まで接近してから撃破された回数。RESULT行に回数と最接近距離を集計
- 実装: Zombie.js（_closestToEscort）・GameScene.js・run_sim.js・simulator.html
- 目安（**※実機照合前の仮当て。後日の実機プレイで感覚と照合して基準に昇格させる**): 0回=楽勝すぎ・設計NG／3〜10回=ノーダメギリギリ・目標／20回超=修羅場すぎ・調整荒い
- 面白さの最終判定は常に小松の実機プレイ。CLOSE_CALLはその感覚を近似する道具にすぎない（spec05_game_identity.md参照）

---

## ■ タワー確定パラメータ（仕様確定済み・実装完了）

balance.json・config.js・simulator.html への実装完了済み（v1.6）。以下が数値の正典。

| type(key) | cost | sell | dmg/発 | 連射間隔 | 射程 | 撃破発数(hp200) | DPS実測 | コスパ実測 | 特性 |
|---|---|---|---|---|---|---|---|---|---|
| normal | **100** | 70 | 50 | 800ms | 250px | 4発 | 62.5 | **62.5** | コスパ最強・量産主力 |
| sniper | **300** | 210 | 34 | 450ms | 350px | 6発 | 75.6 | **25.2** | 広範囲・動的交戦で最強級。DPS/コスパはrange非依存。通過総dmg: 1734 |
| cannon | **200** | 140 | 100 | 1600ms | 300px | 2発 | 62.5 | **31.25** | 一撃必殺・据置き向き |
| ice | **300** | 210 | 25 | 900ms | 250px | 8発 | 27.8 | **9.27** | 削り弱・スロウ（倍率×持続）が本体。シムは通常dmgのみ扱う |
| punch | **100** | 70 | 100 | 2000ms | 100px | 実質1発 | 50.0 | **50.0** | 重い一撃・近接のため要近接buildSpot |

> ※ DPS・コスパは `rangeTest()` 実測値（speed=30px/s・hp=200基準）。コスパ = DPS/cost × 100。売却額=cost×0.7（sellRate固定）。

### アップグレード仕様（v1.6実装済み）

3段階（Lv1=基準無強化・Lv2・Lv3）。Lv1→Lv2の費用 = 設置cost×0.5（Lv2→Lv3は未設定: null）。

| type | dmgMult/段 | fireRateMult/段 | 備考 |
|---|---|---|---|
| normal | ×1.15 | ÷1.15 | dmg・連射ともに向上 |
| sniper | — | ÷1.10 | 連射のみ（dmg・射程固定） |
| cannon | ×1.15 | — | dmgのみ（連射固定） |
| ice | — | ÷1.15 | 連射のみ（dmg固定） |
| punch | — | ÷1.15 | 連射のみ（dmg固定） |

> ※ fireRateMult は **間隔を割る**（÷）= 連射速度が上がる。シム: `upgrade:col,row@ms` で指定時刻にアップグレード実行。

### 設計原則（タワー設計・ステージ設計共通）

1. **単騎完結の禁止**: 1種のタワーだけでどんな交戦地も処理できる性能にしない
2. **交戦地の密度**: スポーン数1体を極力避ける（最低2体以上）
3. **アイスのシム扱い**: スロウ効果はシムに実装しない。三層検証はice抜きで成立させる。実機ではスロウが救済として機能
4. **AoEなし**: 範囲攻撃は実装しない
5. **パンチのbuildSpot**: 射程100pxのため、ステージ設計時に交戦地隣接の近接用buildSpotを意識的に確保すること（**stage_01・segment_test両方でpunchが届くbuildSpotが存在しないことが実測で判明済み。P3量産時の設計チェック項目に含める**）

---

## P2: バランス基盤・設計ガイド更新

量産前に物差しを揃える。

| # | タスク | 内容 | 担当 |
|---|---|---|---|
| 2-1 | 設計ガイド改訂 | 統合版ガイドに**区間制・位置トリガー**/リレー/Y/アップグレード/新敵の設計ルールを追記（F3の古い記述・buildSpots数の食い違いも修正）。新タワー5種の設計思想も反映。**spec05_game_identity.md（人格章）のspec05本体への統合もここで実施**。**spec07（交戦地レシピ）の内容と重複・矛盾しないよう整合を取ること。特に§4.5のタワー処理速度公式はspec07 §1の実測式で更新されている** | Claude→小松承認 |
| 2-2 | **20本の構成台帳【v3.3改訂】** | **作る前に埋める設計図ではなく、作りながら埋まっていく台帳とする。** 1本作るたびに1行埋まり、20本目で完成する。列は従来通り（家族人数構成／導線(A/B)割当／敵種／交戦地の数と密度／アップグレード解禁）に加え、小松の体感（簡単／ちょうどいい／難しい）と、他ステージとの相対順位を記録する。**難易度の数値列は置かない**（v3.1改訂は維持） | 小松×Claude壁打ち |
| 2-3 | チュートリアル設計 | Lv1〜2で何を教えるか（建てる→リレー→アップグレード→Yの順に学習） | 小松×Claude |
| 2-4 | FlowField誤差の長尺検証 | 70セル超ダミーマップで演出spawn誤差を実測（07-10からの宿題③） | CC |
| 2-5 | **タッチ精度・早期Android接触【P3前必須・最重要】** | Capacitorで雛形を作り、AVD or 実機でstage_01を**実際に指でタップ**して確認: ①buildSpotのタップ領域②タッチUIの操作フロー③WebViewでのPhaser 3+Canvas性能。**問題があればbuildSpot設計ルールに制約を追加してP3全体に適用**。**未着手**（2026-08-02時点でのタップ動作確認はPCブラウザ/Edgeのみ、Android実機・AVDでの指タップ確認はまだ行っていない。詳細はリスク登記簿1番参照） | CC×小松 |
| 2-6 | viewportスケーリング設定 | Phaserのscale設定（FIT/ENVELOP等）を明示的に確定 | CC |
| 2-7 ✅ | シム・スイープモード実装 | run_sim.jsに外側ループを追加し、指定パラメータを±刻みで一括実行→1行サマリ表で出力。**CLOSE_CALL回数も出力に含める**。用途は"感度分析"（**良100%達成境界の可視化**）。配置の総当たり探索には使わない | CC |
| 2-8 ✅ | **CLOSE_CALL箇所数の出力** | CLOSE_CALLをスポーン地点別に集計し`[CLOSE_CALL_BY_SPAWN]`として出力。RESULT行に「10回/2箇所」形式で箇所数を追加。既存の`[DAMAGE_BY_SPAWN]`と同じ仕組み。**回数だけでは「1箇所が壊れている」と「盤面全体が緊張している」を区別できないため**（確定事項参照）。3系統すべてに同一実装 | CC |
| 2-9 | **【v3.3新設】資金ルールの修正** | 現在は支給された初期マネーの残りも次の家族へ持ち越されるため、`minSurvivors:1`と組み合わさって「dadとmomをわざと捨てて資金をsonに集中させる」のが有利になる破綻がある。撃破報酬のみ繰り越し、支給分の残りは持ち越さないに変更する。escort交代時に前の家族の支給残は消滅させる | CC |

**スタンプ条件【v3.3改訂】**: **構成台帳の様式が確定し、作成済みステージの行が埋まっている**状態＋stage_01が実機Androidで指で遊べると確認済み＋スイープモードがstage_01で動作する状態。（旧条件「構成表が埋まり次に作るステージの仕様が表から読める」はP2完了をP3着手の前提にしていたため撤回。P2の完了を待たずP3を進められる）

#### 2-2 構成台帳（作成済み2本）

| # | ステージ | 家族人数構成 | 導線 | 敵種 | 小松の体感 | 相対順位 |
|---|---|---|---|---|---|---|
| 1 | 確定ABAステージ（id: stage_free_02） | 3人（dad/mom/son） | A/B/A（共有導線あり） | salaryman/worker/police/kickboard/bird | — | **10**（基準） |
| 2 | 外周ぐるり（id: stage_free_03） | 2人（dad/mom） | 外周のみ（dad時計回り／mom反時計回り） | salaryman/worker/kickboard | 面白さ合格 | **5**（確定ABAステージ=10とした場合） |

### 【P2重要事項・v2.7】護衛speedと難易度の関係が新文法で反転（1-7検証で判明・2026-07-24）

**旧文法（時間駆動spawnEvents）**: 遅い護衛＝現地に長く留まる＝時間経過で湧く敵を多く浴びる＝難しい、という関係だった。

**新文法（segments/位置トリガー）**: 敵の数・出現は護衛のwpIdx到達（位置）で決まり、時間経過そのものには依存しない。護衛が遅くても「浴びる敵の絶対数」は変わらず、変わるのは「タワーが1体を処理できる時間的余裕」だけ。**結果として、護衛speedは新文法では「難易度を下げるパラメータ」として機能する**（遅い＝タワーの処理時間が増える＝易しい）。

1-7の実測でもこれが裏付けられた: 最遅のgrandma(speed35)区間が唯一無傷、最速のcat(speed75)区間が最大被弾だった。

**設計への直接の影響**: 「全escortに同数の敵を配る」という設計は成立しない。旧文法時代の前提のまま2-2カーブ表やステージ設計を進めると、後続の速い家族（son/cat等）が想定以上に苦戦し、遅い家族（grandma等）が想定以上に楽になる歪みが生まれる。**escortごとに敵数・区間密度をspeed差を踏まえて調整する必要がある**（速い護衛には数を絞る／遅い護衛には多めに配置する、等）。2-2カーブ表設計（家族人数構成・敵種の割り付け）はこの前提の上で行うこと。

### 【P3前の議論事項】導線A/B振り分け設計

家族リレーで全員同一導線は体験が単調になるため、ステージ設計時に導線を複数本（A/B）引き、各家族に割り当てる方式を導入する（例: dad=A / mom=B / grandma=A）。S（スタート）とG（ゴール）は全家族共通、その間の経路のみがA/Bに分岐する。

**難易度レバーとしての意味**: 遺産タワー（前の家族が設置）は同一導線の次の家族には効くが、別導線の家族には射程外で無効化される。導線割り当て自体が難易度の設計図となり、資金継続仕様（escort交代でmoney継続）と噛み合う。同導線＝遺産が活きて楽／別導線＝遺産が死んで再構築を強いる。

**2つの導線パターン**:
- セパレート式: A/Bが最後まで交わらない。遺産タワーの有効/無効が完全に二値。家族ごとに独立した難易度を作りたいステージ向き。
- 交わり式: A/Bが途中で合流区間を持つ。合流点は全家族が必ず通る「一等地」となり、リレー全体で最も費用対効果が高い交戦地帯として機能する。資源配分の優先順位を問うステージ向き。合流点タワーもescortのspeed/HP差で「dadには足りるがmomには足りない」が起きうるため、一等地だが万能ではない詰めどころが生まれる（§4.5と整合）。

**データ構造**: エディター内部の編集モデルのみ変更（pathA/pathB共有配列＋各escortはlaneId('A'|'B')で参照）。**書き出されるステージJSON自体は変更なし**——各escortの`path`は従来通り独立した具体配列のまま（エディターがエクスポート時にA/Bの内容を該当escortへコピーするだけ）。ランタイム側（Zombie.js/GameScene.js/run_sim.js/simulator.html）は無改修。

**editor改修 — 実装済み（2026-07-24・CC実装、小松実機確認済み）**: 複数path描画（導線A=シアン・導線B=マゼンタで別色描画）・escort→導線割り当てUI（WAVE設定欄の「このWAVEの導線」ドロップダウン）・A/B両方ともSクリックから開始必須（既存の単一導線と同じ制約を踏襲）を実装。交わり式（A/Bの交差・重複）は専用の「合流点」定義UIを設けず、両レーンが独立配列のため同じマスを両方が通れば自然に交差する形で対応（明示的な合流点マーカーはなし）。既存ファイルの読込時は各escortのpath配列を比較し自動でA/Bを判別・復元する。

**位置づけ**: editorツール自体は完成。どのステージでセパレート式/交わり式のどちらを使うか、遺産タワーの設計への活かし方は引き続き小松判断（次の本番ステージ3-xから適用予定）。ステージ量産（P3）着手前に正式議論する。P2（2-2カーブ表の導線構成割り付け）と直結。

---

## P3: ステージ量産 【最重量・スタンプ20個】

1本＝1スタンプ × 20。

**【v3.6改訂】制作フローの順序を変更——検証より先に実機プレイを置く。**

```
1. ステージ設計 → JSON作成 → R-6（validate_stage.js）を通す → index.json登録
2. 小松が実機でプレイ（面白さの判定・変更フィードバック）
3. フィードバックを反映（必要なら2に戻る）
4. 良100%配置の存在証明
5. P3スタンプ付与
```

**【v3.7改訂】旧5番「雑100%不成立の確認」を判定条件から削除**（詳細は判定基準の改訂を参照）。

**旧フロー（置き換え対象）**: 小松がMAP/導線/交戦地→Claudeが区間設計/buildSpot/タワー案/シム検証→CC実装→小松実機確認（＝検証を先に済ませてから実機確認する順序だった）。

**変更理由**: 「外周ぐるり」（P3 2本目）の制作で、良100%配置の探索に複数回のシム反復を要した後で、実機プレイのフィードバックによる設計変更の可能性が残っている状態になったことを受けての判断（小松）。
- 面白さのフィードバックで設計が変わると、先に済ませた良100%/雑100%の検証がやり直しになる
- 良100%配置の探索はシムの反復が必要で、工程の中で最もコストが高い。設計がひっくり返る可能性がある段階でやるのは無駄が大きい

**留意点**: 実機で「難しすぎてクリアできない」となった場合、設計が悪いのか、良100%配置は存在するが置き方の問題なのかの区別がつかない。その場合はClaudeが配置を1本組んで「これなら通る」ことを示してから判断する。

**【v3.3撤回】作成順序＝逆順生産（ステージ20から作り引き算で下位を作る）は撤回。** 撤回理由: 逆順生産は「天井の姿が分かっていれば、あとは引き算」という前提に立っていたが、難易度が事前に測れない以上、どれが天井かも事前には決められないことが実践で判明した（詳細は下記【P3方針】参照）。

**【v3.3確定】ステージは1本ずつ作り、遊び、体感で相対順位をつける。Lv番号は後から割り振る。** 小松とClaudeが1ステージごとにやり取りしながら作り上げる。難易度は「小松が遊んで簡単か難しいか」で測り、それ以外の物差しは使わない。比較対象が増えるまで絶対的なLv付けは行わない。

- 量産中の鉄則【v3.7改訂】: **良100%クリア可能配置の存在証明が唯一の絶対条件**（spec06 §7準拠）・prop衝突チェック必須・spawn道路上・シム=実機照合はサンプリング（節目ごとに1×実機確認）・**punchが届くbuildSpotの有無チェック**。**実施順序は上記フローの4番（実機プレイでのフィードバック反映後）**
- 新ステージの数値確定はスイープで「良100%達成境界」を確認する
- **並行開始可能**: P3の後半と並行してP4に着手できる

**スタンプ条件【v3.7改訂】**: 各ステージ「良100%配置の存在証明＋実機確認（小松の面白さ判定）＋index.json登録」で1個。20個で完了。**雑100%不成立の確認は条件から削除**（実施は任意・参考記録のみ。理由はspec06_stage_recipe.md §7参照）。

**スタンプ実績 [2/20]**:
- 1本目=確定ABAステージ（id: stage_free_02・2026-08-14確定）。良100%配置（24基2600G・HP残100%全員・撃破46/すり抜け1）、実機確認（PCブラウザ）、index.json登録、いずれも完了（雑100%不成立の確認も当時実施済み・3パターンとも未達）
- 2本目=外周ぐるり（id: stage_free_03・2026-08-16確定）。良100%配置（12基・スポーン22・撃破22・すり抜け0・護衛生還2/2・HP残100%全員）、実機確認（小松プレイ・面白さ合格）、index.json登録、いずれも完了。CLOSE_CALL 0回だが敵の湧く場所の真横にタワーを固めた結果であり、ステージの欠陥ではない（小松実機プレイでは同ステージで8回/7箇所発生）。難易度は確定ABAステージを10とした相対順位で5

### 【P3方針・v3.3改訂】1本ずつ作る・ベースMAP方式（小松判断）

**1本ずつ作る**: ステージは順序を決めずに1本ずつ作り、小松が遊んで体感（簡単／ちょうどいい／難しい）を記録し、他ステージとの相対順位で並べる（2-2の台帳に記録・詳細はP2セクション参照）。比較対象が増えるほど相対順位の精度が上がる設計のため、序盤ほど順位は仮のものとして扱う。

**ベースMAP方式**: ベースとなるMAPを数種類作り、そこから複数ステージを派生させる。
- **固定（ベースMAPで決まる）**: 道路網・導線の骨格、プロップの配置枠（位置・footprint）
- **可変（ステージごとに変える）**: 敵の構成、buildSpotの配置、導線A/B割り当て、プロップの絵柄

**プロップ差し替え原則**: プロップは同サイズ（同footprint）での絵柄差し替えを前提とする。footprintが変わらないため、buildSpotやゾンビの通行判定への影響なしに見た目だけを変えられる。同じベースMAPでも見た目の異なる複数ステージを低コストで作れる。

**要検討（P3着手前）**: ベースMAP数種の具体案、buildSpot可変域の設計ルール（ベースMAPのどこまでを可変にしてよいか）。

---

## P4: メタ構造・外周ゲーム 【P3後半と並行可】

| # | タスク | 内容 | 担当 |
|---|---|---|---|
| 4-1 | セーブ【先に実装】 | クリア状況・ポイント・メタ強化のlocalStorage/端末保存。実装順は必ずセーブが先（4-1→4-2→4-5） | CC |
| 4-2 | ステージ選択画面 | 20ステージ・アンロック進行・星/得点表示 | CC |
| 4-3 | メタ強化ショップ | クリアポイント→恒久強化の購入UI（幅はQ2壁打ちの確定値） | CC |
| 4-4 | ボーナスステージ | ロジック流用ミニゲーム1種。**切り離し可能**（遅延したらリリース後アップデートに回す保険） | 小松×Claude設計→CC |
| 4-5 | リザルト演出 | 生存人数→ポイント演出・星表示。**採点思想（spec05_game_identity.md）に従う: 生存主軸・撃破数は従（桁を分ける）・残マネーは参考表示のみ・ノーダメは美学バッジとして別枠検討** | CC |
| 4-6 | **【v3.3新設】ステージ開始前のルール表示** | ステージ開始前に「家族の順番／各家族の初期マネー／クリア認定人数／資金ルール（撃破分のみ繰り越し）」を表示する。プレイヤーが3人分の計画を先に立てられるようにするのが目的 | CC |

**スタンプ条件**: タイトル→ステージ選択→プレイ→リザルト→ショップ→次ステージ、の一周が通る。

---

## P5: 外装（音・見た目・チュートリアル実装）

| # | タスク | 内容 | 担当 |
|---|---|---|---|
| 5-1 | BGM/SE【再生システム実装込み】 | 素材: Suno等生成AI第一候補→ダメなら素材購入。**実装: BGM再生システム（ループ・フェード）の新規実装が必要** | 素材:小松＋Claude／実装:CC |
| 5-2 | タイトル画面 | 小松「作れる自信ある」 | 小松＋GPT |
| 5-3 | チュートリアル実装 | 2-3の設計をLv1〜2に実装 | CC |
| 5-4 | ゲームフィール | ヒットストップ・ダメージスプライト・パーティクル。**「倒す快感」はここで担保する（採点は静か・触感は派手に、の分担）** | CC |
| 5-5 | 設定画面 | 音量・倍速（1-6修正済み前提でON） | CC |
| 5-6 | **【v3.3新設】リレー交代時の導線提示フェーズ** | 前の護衛のゴール後、次の護衛の出発前に停止フェーズを挟む。次の護衛の導線をスタートからゴールまで矢印で走らせ、「よーいスタート」で開始。このフェーズ中は建設不可（情報開示のみのターン）。導線が変わる家族が実質初見で放り出される理不尽の解消と、リレーの区切りの演出化を兼ねる | CC |

**スタンプ条件**: 音が鳴り、気持ちよく、初見が迷わない。

---

## P6: Android化・ストア・リリース

| # | タスク | 内容 | 担当 |
|---|---|---|---|
| 6-1 | Android化本組み | Capacitorほぼ一択（CC所見）。2-5の雛形をベースに本ビルド構成確定 | CC×小松 |
| 6-2 | 実機/AVD検証 | タッチ操作・解像度・パフォーマンス | 小松 |
| 6-3 | ストア準備 | Google Play Console・買い切り300円・アイコン/スクショ/説明文 | 小松 |
| 6-4 | 審査→リリース | 審査リジェクト対応バッファを見込む | 小松 |
| 6-5 | X開発ログ着手 | 「素人がゲームを作る」ナラティブ（90%完成後着手＝P5あたりから始動） | 小松 |

**スタンプ条件**: ストア公開。🎉

---

## 分業マップ

| 役割 | 担当領域 |
|---|---|
| 小松 | ディレクション・MAP/導線/交戦地決定・実機確認・**面白さ判定（GO/NO-GO判定を含む最終判定者）**・タイトル・ストア・X・**シム実行**（トークン＆時間節約のため） |
| Claude（僕/後続インスタンス） | 仕様壁打ち・数値設計/検算・シム設計（ステージJSON＋タワー配置案を渡すまで。実行は小松）・**ガイド/工程表/人格章の維持**・発注文作成 |
| CC（＋Opus/Sonnet） | 全実装（実機＋シム同時が鉄則）・バグ根本調査・**工程表の機械的更新（下記権限ルール参照）** |
| GPT | スプライト・画像素材（新敵3種・タイトル素材・ストア画像） |

### 工程表の更新権限ルール（v2.0新設）

工程表はリアルタイム更新とし、更新権限を以下の通り分ける:

**CCが直接更新してよいもの（機械的更新）**:
- タスクのチェックボックス（未完了→✅）
- スタンプ進捗カウンタの数字
- 「現在地」セクションの一行（「次は◯◯」）
- バージョン番号・日付・変更点欄への実装事実の記載

**CCが単独で書き込まないもの（判定・解釈を含む更新）**:
- GO/NO-GO判定の記録（実装完了≠タスク完了。特に1-4.5等の実証実験タスクは小松の実機判定を経るまで✅にしない。判断が必要な場面ではチェックを保留のまま完了報告だけ送る）
- リスク登記簿の順位変更
- スタンプ進捗が後退した場合の解釈・理由づけ
- 確定事項・設計原則・採点思想の変更

**シム運用ルール**: ClaudeはステージJSONとタワー配置パターンを渡し、小松がブラウザシム（http://127.0.0.1:5500/simulator.html）で実行してRESULT行を貼り返す。ClaudeがNode.js等でシムを自走させるのは原則禁止。

**セッション引き継ぎ（正典5本＋JSON）**: 「工程表＋spec05（設計ガイド）＋spec05_game_identity.md（人格章）＋spec06_stage_recipe.md（ステージレシピ）＋**spec07_engagement_recipe.md（交戦地レシピ）**＋file_structure_review.md＋最新ステージJSON」で次のインスタンスが即続行できる状態を常に維持する。**file_structure_review.mdの実行タイミング一覧は「現在地」更新時に必ず照合し、条件が満たされたタスク（R-1〜R-4）がないか確認すること。**

---

## リスク登記簿（工程を脅かす順）

1. **タッチ精度（CELL=100px×Android）** — 論理2000pxを物理画面に圧縮したときbuildSpotが指で押せない可能性。P3の20本を作ってからP6で発覚すると全やり直し。対策: 2-5でP3前にstage_01を実機/AVDで指タップ確認。**2026-08-02時点で2-5は未着手**——本日の動作確認はPCブラウザ（Edge）のみで、Android実機・AVDでの指タップ確認はまだ行っていない。
   **【2026-08-02】タップ処理で3件の不具合が発生・解決**（いずれもタッチ操作の根幹に関わるため記録）: ①`popupJustActed`が「次の1タップで解除」というイベントカウント方式で、想定のゴーストタップが来ないと永久固着 → 実時間300msロックに変更 ②UISceneとGameSceneが別々のInputPluginを持ち同一タップが両方に届いていたため、ポップアップのボタンを押すとGameScene側が誤認してトグルクローズしていた ③`_tryPlace()`の`_canPlace`失敗パスが`_closePopup()`を通らず`input.enabled`がfalseのまま固着。最終解: `update()`先頭で`this.input.enabled = !this.popupState;`と毎フレーム同期する方式に一元化し、建設・方向選択・売却/アップグレードの3ポップアップをすべてUIScene側に統一。「フラグを立てて別の場所で戻す」設計は戻し忘れで必ず固着するため、状態から毎フレーム導出する形に変更した。
2. ~~新文法転換の検証未了（v2.0新設）~~ **【解消済み・2026-07-19】** — 1-4.5がGO確定（良100%配置の存在証明＋雑100%不成立4パターンを確認、小松実機判定）。当初のリスクは「GO/NO-GO判定が未確定のままP1後半に進むと手戻り」だったが、判定確定によりこの前提が解消。記録として残す（他リスクの解消判定時の参考に）
3. **P1の仕様壁打ち不足** — Y再定義（1-4）の仕様が甘いままP3に入ると20本作り直し。対策: 1-7の全部載せ検証を必ず通す
4. **鳥系ゾンビ（1-5）** — 飛行セル次第でbuildSpot制約が変わりステージ設計に影響。対策: P3前に1体実装して挙動確認。重すぎれば「後半Lvのみ登場」で依存を切る
5. **P3の調整量** — シム工場があっても19本は量。対策: カーブ表(2-2)で仕様を先に固定し、1本あたりの意思決定を減らす
6. **メタ強化×難易度の掛け算（Q2）** — 対策: 「強化ゼロで全ステージ成立」原則を壁打ちで確定し、調整変数から外す
7. **シム2系統の乖離（v2.0新設）** — run_sim.js（CLI）とsimulator.html（ブラウザ）でスタブ・状態管理の片側修正事故が既に2回発生（Phaserスタブ漏れ・状態汚染・vm束縛バグ）。対策: 修正は必ず両系統同時、可能ならスタブ・コアの共通化を検討（1-4.5クローズ後）
8. ~~実機側のprop衝突判定が未実装（v3.1新設）~~
   **【解消済み・2026-07-29】** — GameScene.jsの`_canPlace()`にprop衝突ガードを追加し、`_placeInitialTowers()`とエラーフォーマットを統一。`_propBlocked`をSet→Mapに変更してシムと同じラベルを保持。**2経路の判定基準が一致し、シム=実機の穴は塞がった**（コミットc30f743）。stage_test_06のprop衝突18箇所すべてがブロックされることを確認済み。記録として残す（他リスクの解消判定時の参考に）
9. ~~JSONの書き損じが実行結果からしか発覚しない（v3.3新設）~~
   **【解消済み・2026-08-11】** — 根本対策のR-6（validate_stage.js）が3便構成で完了（コミットe149600/cebd8d0/cb9b9e1、docs反映5d21bef）。座標・ルール突き合わせ9項目＋枠外spawn侵入余裕チェック＋`--margin`猶予マップ＋file_structure_review.md原文の残り3項目（buildSpots×prop衝突・spawnキー未定義参照・leashTo歩行可能性）の計13項目を実装。確定ABAステージで全項目OKを確認済み、各項目とも意図的に壊したテストJSONでERROR/WARNが正しく出ることを確認済み。記録として残す（他リスクの解消判定時の参考に）

---

## 現在地

**P2進行中・P3 2本目（外周ぐるり）スタンプ確定（2026-08-16）。**

**外周ぐるり（id: stage_free_03）、P3スタンプ条件（v3.7改訂後）を全て満たし確定。**
- 良100%配置の存在証明: 12基でスポーン22・撃破22・すり抜け0・護衛生還2/2・HP残100%（dad100/mom100）・判定CLEAR
- 実機確認: 小松のプレイで完了。面白さ合格、難易度は確定ABAステージを10とした相対順位で5
- index.json登録済み（id:12）

**本日（2026-08-16）実施した内容**:
- **判定基準を改訂（小松判断）**: 雑100%不成立の確認とCLOSE_CALL 0回=設計NGを、どちらも合否判定から外した。理由は両方ともプレイヤーの配置次第で動く数字で、ステージ自体の性質を測れないため（詳細はspec06_stage_recipe.md §7・spec05_game_identity.md参照）。判定基準は「良100%配置の存在証明」の一点に統合
- 外周ぐるりのP3スタンプ付与（2本目）。2-2構成台帳に1本目・2本目の行を追加

**次にやること**:
1. **3本目のステージ制作**

**設計面の主戦場は交戦地レシピ（spec07）**——ステージ設計を「配置を考える」から「型を選んで刺す」に変える試み。initial型のカタログと必要基数の計算式は実ステージで検証済み。trigger型は「スポーン後2秒の無敵」の発覚により再構築中で、**Gから来る型（ゴール方向から敵を出し正面衝突させる）の正解タワー位置公式が敵speed 2種で的中**、stage_test_06への適用も成功している。**未解決**: ①公式の3点目（鳥・speed70）の検証②mom/sonへの適用（家族speedが違うため正解位置が3箇所に分かれる）。**【2026-08-16更新】**「Gから来る型でキックボードのCLOSE_CALLが0回になり調整必須」としていた旧未解決事項①は、CLOSE_CALL 0回=設計NGの運用自体が破棄されたため解消（対応不要）。

**ドキュメント整合**: 2026-07-29に正典5本を原文突合し、CLOSE_CALLの目安値の誤引用（spec07 §3.7）を修正済み。2026-08-16に判定基準改訂に伴いspec06_stage_recipe.md §7・spec05_game_identity.mdのCLOSE_CALL記述を更新。

*スタンプ進捗: P0 [4/4✅] | P1 [10/10✅] | P2 [2/9] | P3 [2/20] | P4 [0/6] | P5 [0/6] | P6 [0/5] — 総計 18/60*

---

## Ver2以降検討（現在スコープ外・ペンディング）

| 項目 | 内容 | 見送り理由 |
|---|---|---|
| 対空カテゴリ | 対空タワー＋対空専用敵ライン | 地上ロジックとの分離コスト大 |
| タワー相性（弱点） | 敵タイプ別タワー弱点（dmg倍率） | シム三層検証の複雑度が上がる |
| 護衛シナジー | 護衛variant別の周辺効果 | 3軸掛け算でバランス管理が指数的に重くなる |
| 残マネーのスコア化 | リザルトでの効率ボーナス点 | 初期リリースは参考表示のみ。反応を見てVer2で検討（採点思想はspec05_game_identity.md参照） |
| 退場ゾンビの小芝居 | retreat前のキョロキョロ等のコミカル演出 | 1-4.5スコープ外。GO後にP5-4で検討 |

---

## ペンディング事項（結論待ち・v3.3新設）

**この文書の役割**: 議論はしたが結論に至っていない項目を記録し、失われないようにする。Ver2以降検討（上記表）とは異なり、「見送り」が確定したわけではなく、単に結論待ちの項目を置く場所。

| 項目 | 内容 | 状態 |
|---|---|---|
| タワー種の差別化 | 小松の指摘「タワー種の違いが薄すぎる」。実測でもcannon/punchは機能しておらず、normal/sniperで事足りる。punchは「敵の通り道を押さえる」タワーだがESCORT TDには通り道が存在しないため役割の前提が消えている | 結論出ず・ペンディング |
| 新タワー案 | 全方位電撃／直線レーザー／粘着STOP／火災壁／3発弾／ファイヤーボム（STOPして任意マスに置く使い切りの地雷）。うちレーザーは「敵は護衛へ放射状に集まるため線にならない」ことが判明し相性が悪い | 結論出ず・ペンディング |
| 敵の耐性（ジャンケン） | 「normal/sniperがほぼ効かないが炎はよく通る」等の耐性導入案。ただし「良100%配置はnormal+sniperで成立する」という保証ラインと矛盾することが判明（既存の「タワー相性（弱点）」のVer2検討項目と同じ場所に到達） | 矛盾未解決・ペンディング |
| アイテム枠の新設 | ファイヤーボムやサテライト等をタワーとは別カテゴリで扱う案 | **却下済み**（小松判断で「新設しない（完成が遠のく）」と決定） |
| 情報開示の方式 | 小松案: システムが攻略情報を全開示するのではなく、プレイヤーが自ら情報を確認できる仕組みがよい。具体案は「そのターンだけプレイ時間をSTOPできる仕組み（シューティングゲームの爆弾のようなもの）」 | 結論出ず・ペンディング |
| 単元制のステージ構成 | 小松案: 20ステージを5つの塊に分け、各塊に1テーマ（基本編／敵種増える編／タワー増える編／Y実装編／総まとめ編）。順番は「基本→normal/sniperで倒しづらい敵を出して苦労させる→新タワー投入」が一番きれい。新タワーの渡し方は「同じ面で／次の面で／クリア報酬で」の3案 | 結論出ず・ペンディング |
| スパイスの2層 | 飽きを防ぐ要素はアンロック型（タワー・敵・システムが増える）と体験型（同じ道具で別の要求。Yは「待機型防衛TDに変わる」という意味で体験型スパイス）の2層に分かれる | 記録のみ |

---

## 更新ルール

- タスク完了時: 該当行の状態を ✅ に変更 → スタンプ進捗カウンタを更新 → 「現在地」セクションを書き換え（更新権限ルールに従う）
- フェーズ完了時: ファイル冒頭のバージョン番号と日付を更新
- セッション引き継ぎ: 正典5本＋JSON（工程表＋spec05（設計ガイド）＋spec05_game_identity.md（人格章）＋spec06_stage_recipe.md（ステージレシピ）＋spec07_engagement_recipe.md（交戦地レシピ）＋file_structure_review.md＋最新ステージJSON）で次インスタンスが即続行できる状態を常に維持する
